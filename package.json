{
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "poi": "^12.9.0",
    "stylus": "^0.54.8",
    "stylus-loader": "^3.0.2"
  },
  "dependencies": {
    "vue": "^2.6.11",
    "vue-lazyload": "^1.3.3",
    "vue-resource": "^1.5.1",
    "vue-template-compiler": "^2.6.11"
  },
  "scripts": {
    "dev": "poi --serve",
    "build": "poi --prod",
    "deploy": "npm run build && gh-pages -d dist -b master"
  }
}
